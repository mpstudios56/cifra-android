<?xml version="1.0" encoding="utf-8"?>
<!--
  What has been deleted, and for how much longer it will still be here.

  The line at the top is the part that matters: a bin nobody knows will empty
  itself is a bin nobody trusts, and one that never empties is a second copy of
  the database growing quietly in the corner.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/trash_base"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/trash_note"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingTop="14dp"
        android:paddingEnd="16dp"
        android:paddingBottom="12dp"
        android:textAppearance="?android:attr/textAppearanceSmall" />

    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <TextView
        android:id="@android:id/empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:padding="24dp"
        android:text="@string/trash_empty" />

    <Button
        android:id="@+id/trash_empty_now"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_margin="16dp"
        android:background="@drawable/donate_button"
        android:text="@string/trash_empty_now"
        android:textColor="#FFFFFFFF"
        android:textStyle="bold" />
</LinearLayout>
