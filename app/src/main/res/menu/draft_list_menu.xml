<?xml version="1.0" encoding="utf-8"?>
<!-- Shown while drafts are selected, so it only offers what applies to a selection. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/delete_selected_drafts"
        android:icon="@drawable/ic_action_trash"
        android:title="@string/delete"
        app:showAsAction="ifRoom|withText" />

    <item
        android:id="@+id/select_all_drafts"
        android:title="@string/select_all_drafts"
        app:showAsAction="never" />
</menu>
