package io.github.mpstudios56.cifra.bus;

/**
 * Take me back to the menu.
 * <p>
 * Older than {@link SwitchToTab} and kept because a few screens still send it:
 * the menu is where one lands after finishing with something opened from it.
 */
public class SwitchToMenuTabEvent {
}
